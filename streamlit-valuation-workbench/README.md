# Valuation Workbench

A public/private company DCF model. The public-company workflow loads annual reported financials from Financial Modeling Prep using the visitor's own API key; the private-company workflow uses manual due-diligence inputs.

Run locally with:

```bash
streamlit run app.py
```
